lookupscli is a tool to process songs data

```bash
lookupscli    # it's a scriptable command line program
   -g         # Performs a RDAP lookup on the list_of_ips
   -r         # Performs a Geo IP lookup on the list_of_ips  
```